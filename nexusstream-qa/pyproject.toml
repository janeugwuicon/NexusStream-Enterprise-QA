[project]
name = "qa-playwright-framework"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pytest",
    "playwright",
    "pytest-playwright",
    "allure-pytest",
    "pytest-xdist>=3.8.0",
    "pytest-sugar>=1.1.1",
    "pytest-rerunfailures>=16.1",
]
